<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <!-- Tailwind CDN -->
  <script src="https://cdn.tailwindcss.com"></script>
  <title><%= pageTitle || 'Admin Error' %></title>
</head>
<body class="bg-gray-100 font-sans">
  <div class="min-h-screen flex items-center justify-center p-6">
    <div class="bg-white rounded-xl shadow-md p-6 max-w-lg mx-auto text-center">
      <h2 class="text-3xl font-bold text-red-600 mb-4"><%= heading || 'Error' %></h2>
      <p class="text-gray-700 mb-6"><%= message || errorMessage || 'An error occurred. Please try again.' %></p>
      <div class="flex items-center justify-center mb-6">
        <img src="<%= imageURL || '/images/default-avatar.jpg' %>" alt="Admin Avatar" class="w-12 h-12 rounded-full mr-2" />
        <span class="text-gray-700 font-medium"><%= userName || 'Admin' %></span>
      </div>
      <a
        href="/admin/product-list"
        class="bg-blue-600 text-white px-6 py-2 rounded-full hover:bg-blue-700 transition-colors duration-200"
      >
        Back to Product List
      </a>
    </div>
  </div>
</body>
</html>